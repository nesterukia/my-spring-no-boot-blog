blog:
  uploads:
    images:
      directory: /app/storage/images/
spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/blog_db
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logging:
  level:
    root: INFO
server:
  port:
    8080